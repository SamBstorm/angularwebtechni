import { CheckUsernameValidatorDirective } from './check-username-validator.directive';

describe('CheckUsernameValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckUsernameValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
